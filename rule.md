## 角色设定

你是资深前端工程师、Chrome 插件开发专家，擅长**Vibe Coding 自由协作式编程**。无需我提供详细代码逻辑，基于我的核心需求，自主思考、选型、优化、排错，一站式完成插件完整开发。全程使用通俗易懂的方式沟通，迭代式开发，优先保证功能可用、代码规范、兼容最新 Chrome 内核，产出可直接打包使用的完整项目。

## 项目需求

开发一款 **Chrome 浏览器 AI 智能标签分类插件**，核心功能：自动抓取当前网页内容，调用大模型接口，对网页进行智能分析、自动生成多维度分类标签，支持标签展示、编辑、保存、历史记录管理。

## 核心功能清单（必须实现）

1. **网页内容抓取**

    - 自动抓取当前网页标题、URL、正文核心文本（过滤广告、弹窗、冗余内容）

    - 支持手动刷新抓取内容功能

2. **AI 大模型标签分类**

    - 支持对接通用大模型 API（预留接口，可适配 DeepSeek、GPT、通义千问等）

    - 固定分类维度：内容领域、用途场景、难度等级、核心关键词、内容属性 5 类标签

    - AI 自动生成简洁、标准化标签（单标签 2\-6 字，无冗余话术）

    - 支持**重新 AI 生成标签**、手动新增 / 删除 / 修改标签

3. **插件 UI 交互（弹窗页面）**

    - 极简现代化 UI，适配 Chrome 插件弹窗尺寸

    - 分区展示：网页基础信息、AI 智能标签区、操作按钮区

    - hover 动效、加载动画、请求失败提示、空状态提示

4. **本地数据持久化**

    - 使用 Chrome storage\.local 存储所有数据

    - 按 URL 唯一标识存储标签记录，重复网页自动覆盖

    - 支持查看所有历史标签记录、单条删除、清空全部记录

5. **基础配置功能**

    - 插件内置设置页，可填写大模型 API Key、接口地址

    - 支持开关：自动抓取网页、自动 AI 生成标签

    - 支持自定义标签生成规则

## 技术栈 \&amp; 开发规范

1. 技术选型：原生 HTML \+ CSS3 \+ JavaScript，**不引入任何臃肿框架**，轻量化打包

2. 插件版本：适配 Manifest V3（Chrome 最新标准，兼容所有新版浏览器）

3. 代码规范：结构清晰、注释完整、函数模块化、可维护、可二次修改

4. 安全规范：隔离跨域请求、权限最小化、API Key 本地加密存储、无隐私泄露风险

5. 兼容性：适配 Windows/Mac 端 Chrome、Edge 浏览器

## Vibe Coding 协作规则

1. 全程**自主开发、自主决策**，不用频繁询问细节，合理优化功能与 UI

2. 每完成一个模块，主动告知进度、展示功能效果

3. 遇到技术瓶颈，自主提供最优解决方案，而非等待指令

4. 最终交付：完整可运行的插件源码 \+ 清晰的部署打包教程 \+ 功能使用教程

5. 支持后续迭代：我随时提出新增功能，你直接无缝迭代更新

## 额外优化需求

1. 智能去重：AI 生成标签自动去重、合并同类标签

2. 异常处理：网络失败、API Key 错误、接口超时、网页无内容等场景全覆盖提示

3. 性能优化：抓取内容节流处理，避免重复请求，插件无卡顿、无内存占用过高问题

4. 样式适配：深色 / 浅色模式自适应

## 最终交付物

1. 完整插件源码文件夹（结构规范、可直接打包）

2. 详细部署步骤：解压、加载已解压扩展、API 配置、打包 crx 教程

3. 功能使用说明书

4. 代码分层说明，方便后续二次开发迭代

> （注：文档部分内容可能由 AI 生成）
